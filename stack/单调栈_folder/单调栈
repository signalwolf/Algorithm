1. 单调递减栈去除掉的是波谷，而单调递增栈去掉的是波峰
2. 单调栈可以完成的项目：
    1.  左边区间第一个比它小的数（单调递增），第一个比它大的数（单调递减）
    2.  确定这个元素是否是区间最值
    3.  右边区间第一个大于它的值到 右边区间第一个大于它的值 的距离
    4.  确定以该元素为最值的最长区间
3. 为任意一个元素找左边和右边第一个比自己大/小的位置，用单调栈